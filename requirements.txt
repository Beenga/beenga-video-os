# Mirrors the pip install in Dockerfile. Kept at repo root because RunPod's
# GitHub integration expects the worker-basic layout (Dockerfile,
# rp_handler.py, requirements.txt at root) and scans there.
torch==2.6.0
torchvision
torchaudio
opencv-python-headless>=4.9.0.80
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio[ffmpeg]
imageio-ffmpeg
easydict
ftfy
numpy>=1.23.5,<2
librosa
soundfile
huggingface_hub
einops
decord
loguru
omegaconf
peft
hf_transfer
runpod
